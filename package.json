{
  "name": "vita-product-boilerplate",
  "private": true,
  "scripts": {
    "build": "turbo run build",
    "build:web": "turbo run build --filter=web...",
    "build:mobile": "turbo run build --filter=@repo/mobile...",
    "dev": "turbo run dev",
    "dev:web": "turbo run dev --filter=web...",
    "dev:mobile": "turbo run dev --filter=@repo/mobile...",
    "lint": "turbo run lint",
    "lint:web": "turbo run lint --filter=web...",
    "lint:mobile": "turbo run lint --filter=@repo/mobile...",
    "generate": "turbo run generate",
    "generate:web": "turbo run generate --filter=web...",
    "generate:mobile": "turbo run generate --filter=@repo/mobile...",
    "dev:codegen": "pnpm --filter @repo/graphql dev",
    "format": "prettier --write \"**/*.{ts,tsx,md}\"",
    "check-types": "turbo run check-types",
    "check-types:web": "turbo run check-types --filter=web...",
    "check-types:mobile": "turbo run check-types --filter=@repo/mobile...",
    "test": "turbo run test",
    "test:web": "turbo run test --filter=web...",
    "test:mobile": "turbo run test --filter=@repo/mobile...",
    "prepare": "husky",
    "release": "standard-version",
    "release:minor": "standard-version --release-as minor",
    "release:major": "standard-version --release-as major",
    "release:patch": "standard-version --release-as patch",
    "release:first": "standard-version --first-release"
  },
  "devDependencies": {
    "@commitlint/cli": "^20.2.0",
    "@commitlint/config-conventional": "^20.2.0",
    "@repo/config": "workspace:*",
    "husky": "^9.1.7",
    "lint-staged": "^16.2.7",
    "prettier": "^3.7.4",
    "standard-version": "^9.5.0",
    "turbo": "^2.7.1",
    "typescript": "^5.9.3"
  },
  "packageManager": "pnpm@9.0.0",
  "engines": {
    "node": ">=20",
    "pnpm": ">=9"
  }
}
