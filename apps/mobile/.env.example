# Mobile App Environment Variables
# ===========================================
# 
# IMPORTANT: Run `pnpm setup:env` to auto-detect your IP address
# Or manually set EXPO_PUBLIC_API_HOST below
#
# ===========================================

# API Configuration
# -----------------------------------------
# Option 1: Auto-detect (recommended)
# Run: pnpm setup:env
# This will automatically set EXPO_PUBLIC_API_HOST to your local IP

# Option 2: Manual configuration
# For local development (iOS Simulator on Mac):
# EXPO_PUBLIC_API_HOST=localhost

# For physical devices or different networks:
# EXPO_PUBLIC_API_HOST=192.168.15.7  # Replace with your machine's IP

# Option 3: Full URL override (for tunnels, ngrok, etc.)
# EXPO_PUBLIC_API_URL=http://your-tunnel-url.ngrok.io/graphql

# Default values (will be set by setup:env if not specified)
EXPO_PUBLIC_API_HOST=localhost
EXPO_PUBLIC_API_PORT=4000

# Metro Bundler Configuration (optional)
# EXPO_METRO_PORT=8081
# REACT_NATIVE_PACKAGER_HOSTNAME=192.168.15.7

# ===========================================
# Multi-Environment Setup
# ===========================================
# 
# If you work in multiple environments (home, office, etc.):
# 
# 1. Run `pnpm setup:env` before starting the app
#    This will auto-detect and update your IP
#
# 2. Or create environment-specific .env files:
#    - .env.home (for home network)
#    - .env.office (for office network)
#    - Copy the appropriate one to .env before starting
#
# 3. Or use EXPO_PUBLIC_API_URL for a stable endpoint:
#    - Set up a tunnel (ngrok, cloudflared, etc.)
#    - Use the tunnel URL in EXPO_PUBLIC_API_URL
#    - This works regardless of your local IP
#
# ===========================================
