# ===========================================
# API Environment Variables (NestJS)
# ===========================================
# Copy this file to .env in apps/api/
# ===========================================

# Server Port (default: 4000 for GraphQL APIs)
PORT=4000

# Database Connection (PostgreSQL default port: 5432)
# Format: postgresql://USER:PASSWORD@HOST:PORT/DATABASE
DATABASE_URL=postgresql://postgres:postgres@localhost:5432/vita_dev
